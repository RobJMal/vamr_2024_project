# Description: Parameters for the pipeline

initialization:
  # parameters for the initialization module
  bootstrap_frames: [0, 2]

# module: keypoint_tracking
keypoint_tracking:
  # parameters for the keypoint tracking module
  param_1: 0.5 
  param_2: 0.5 # just examples
  